# 08 - 基础设施、部署与研究工具深度分析

> 学习顺序：第 8 篇 | 前置知识：Docker、Python 打包、并发编程、定时任务

---

## 一、模块概述

基础设施层位于项目最底层，承担：
- 零循环依赖的常量层
- 统一的路径解析与 Profile 隔离
- 安全的并发与容错
- 可观测性
- 跨平台兼容

### 核心组成
```
┌─────────────────────────────────────────────────────────────┐
│                     INFRASTRUCTURE LAYER                     │
├──────────────────┬──────────────────┬────────────────────────┤
│  常量与路径管理  │  日志系统        │  定时任务调度           │
│  环境检测        │  脱敏与过滤      │  多平台投递             │
│  网络偏好        │  会话上下文      │  容错与防重             │
├──────────────────┼──────────────────┼────────────────────────┤
│  批处理系统      │  轨迹压缩        │  部署与容器化           │
│  并行执行        │  上下文压缩      │  Docker镜像             │
│  checkpointing   │  摘要生成        │  非root运行            │
└──────────────────┴──────────────────┴────────────────────────┘
```

---

## 二、配置与常量管理

### 2.1 路径解析

`hermes_constants.py` 提供统一的路径解析机制，支持多平台和多Profile隔离。

```python
# 核心实现
def get_hermes_home() -> Path:
    return Path(os.getenv("HERMES_HOME", Path.home() / ".hermes"))

def get_default_hermes_root() -> Path:
    native_home = Path.home() / ".hermes"
    env_home = os.environ.get("HERMES_HOME", "")
    if not env_home:
        return native_home
    env_path = Path(env_home)
    try:
        env_path.resolve().relative_to(native_home.resolve())
        return native_home
    except ValueError:
        pass

    if env_path.parent.name == "profiles":
        return env_path.parent.parent
    
    return env_path
```

**设计特点：**
- 支持环境变量 `HERMES_HOME` 自定义根目录
- 自动识别Profile目录结构：`~/.hermes/profiles/<name>`
- 路径解析兼容Docker部署：当`HERMES_HOME`指向自定义路径时正确处理
- 提供友好的显示路径：自动转换为`~/`开头的简洁格式

### 2.2 渐进式迁移策略
```python
def get_hermes_dir(new_subpath: str, old_name: str) -> Path:
    home = get_hermes_home()
    old_path = home / old_name
    if old_path.exists():
        return old_path
    return home / new_subpath
```
- 新安装使用统一布局：`cache/images`、`data/trajectories`等
- 已有安装保持旧路径：`image_cache`、`trajectories`等
- 零迁移成本，完全向后兼容

### 2.3 Profile 多实例隔离
```python
def get_subprocess_home() -> str | None:
    hermes_home = os.getenv("HERMES_HOME")
    if not hermes_home:
        return None
    profile_home = os.path.join(hermes_home, "home")
    if os.path.isdir(profile_home):
        return profile_home
    return None
```
- 每个Profile的 `HERMES_HOME` 指向 `~/.hermes/profiles/<name>`
- `get_subprocess_home()` 为git/ssh/gh等子进程提供独立HOME目录
- 子进程的配置、密钥、缓存完全隔离，不污染系统环境
- Python进程自身环境不修改，仅在子进程启动时注入

### 2.4 跨平台环境检测
```python
def is_termux() -> bool:
    prefix = os.getenv("PREFIX", "")
    return bool(os.getenv("TERMUX_VERSION") or "com.termux/files/usr" in prefix)

def is_wsl() -> bool:
    global _wsl_detected
    if _wsl_detected is not None:
        return _wsl_detected
    try:
        with open("/proc/version", "r") as f:
            _wsl_detected = "microsoft" in f.read().lower()
    except Exception:
        _wsl_detected = False
    return _wsl_detected

def is_container() -> bool:
    global _container_detected
    if _container_detected is not None:
        return _container_detected
    if os.path.exists("/.dockerenv"):
        _container_detected = True
        return True
    if os.path.exists("/run/.containerenv"):
        _container_detected = True
        return True
    try:
        with open("/proc/1/cgroup", "r") as f:
            cgroup = f.read()
            if "docker" in cgroup or "podman" in cgroup or "/lxc/" in cgroup:
                _container_detected = True
                return True
    except OSError:
        pass
    _container_detected = False
    return False
```
- **Termux检测**：通过环境变量`TERMUX_VERSION`或`PREFIX`路径
- **WSL检测**：读取`/proc/version`判断是否包含微软标识，结果缓存
- **容器检测**：检查`/.dockerenv`、`/run/.containerenv`或cgroup路径
- 所有检测方法均为导入安全，无额外依赖，可在任何模块中使用

### 2.5 网络偏好与IPv4优先级
```python
def apply_ipv4_preference(force: bool = False) -> None:
    if not force:
        return

    import socket
    if getattr(socket.getaddrinfo, "_hermes_ipv4_patched", False):
        return

    _original_getaddrinfo = socket.getaddrinfo

    def _ipv4_getaddrinfo(host, port, family=0, type=0, proto=0, flags=0):
        if family == 0:
            try:
                return _original_getaddrinfo(
                    host, port, socket.AF_INET, type, proto, flags
                )
            except socket.gaierror:
                return _original_getaddrinfo(host, port, family, type, proto, flags)
        return _original_getaddrinfo(host, port, family, type, proto, flags)

    _ipv4_getaddrinfo._hermes_ipv4_patched = True
    socket.getaddrinfo = _ipv4_getaddrinfo
```
**设计原理：**
- Monkey-patch系统`socket.getaddrinfo`函数，在请求未指定协议族时优先使用IPv4
- 解决IPv6网络不可用或配置错误导致的TCP连接超时问题
- 安全无侵入：仅在配置`network.force_ipv4: true`时启用
- 幂等设计：多次调用不会重复patch，避免副作用
- 优雅降级：如果IPv4解析失败，自动回退到原解析逻辑，支持纯IPv6环境

---

## 三、日志系统

日志系统采用分层设计，支持多环境部署，自动脱敏敏感信息，提供会话级上下文跟踪。

### 3.1 架构设计
```
┌─────────────────────────────────────────────────────────────┐
│                        LOGGING SYSTEM                        │
├─────────────────────────────────────────────────────────────┤
│  三个日志文件                                                │
│  • agent.log: INFO+ 所有Agent活动、工具调用、会话流程       │
│  • errors.log: WARNING+ 错误与警告，用于快速问题排查         │
│  • gateway.log: INFO+ 网关专属日志，消息收发、平台交互       │
├─────────────────────────────────────────────────────────────┤
│  核心特性                                                    │
│  • RotatingFileHandler 自动轮转，防止日志文件过大           │
│  • RedactingFormatter 敏感信息脱敏，API密钥不落盘           │
│  • 会话上下文注入，每线程独立session_id跟踪                 │
│  • 第三方库噪音抑制，默认将15+个库日志级别设为WARNING        │
│  • 托管模式权限适配，支持多用户共享部署                      │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 会话上下文实现
```python
# 线程本地存储
_session_context = threading.local()

# 公开API
def set_session_context(session_id: str) -> None:
    _session_context.session_id = session_id

def clear_session_context() -> None:
    _session_context.session_id = None

# 日志记录工厂注入
def _install_session_record_factory() -> None:
    current_factory = logging.getLogRecordFactory()
    if getattr(current_factory, '_hermes_session_injector', False):
        return

    def _session_record_factory(*args, **kwargs):
        record = current_factory(*args, **kwargs)
        sid = getattr(_session_context, 'session_id', None)
        record.session_tag = f" [{sid}]" if sid else ""
        return record

    _session_record_factory._hermes_session_injector = True
    logging.setLogRecordFactory(_session_record_factory)

# 模块导入时自动安装
_install_session_record_factory()
```
**设计亮点：**
- 使用`threading.local()`实现线程安全的会话上下文存储
- 替换全局`LogRecordFactory`，确保所有日志记录都包含`session_tag`字段
- 与日志格式解耦，任何Handler都可以使用`%(session_tag)s`格式占位符
- 对第三方库日志同样生效，实现全链路会话跟踪
- 幂等设计，模块重新加载不会重复安装

### 3.3 组件级日志过滤
```python
class _ComponentFilter(logging.Filter):
    def __init__(self, prefixes: Sequence[str]) -> None:
        super().__init__()
        self._prefixes = tuple(prefixes)

    def filter(self, record: logging.LogRecord) -> bool:
        return record.name.startswith(self._prefixes)

# 组件前缀定义
COMPONENT_PREFIXES = {
    "gateway": ("gateway",),
    "agent": ("agent", "run_agent", "model_tools", "batch_runner"),
    "tools": ("tools",),
    "cli": ("hermes_cli", "cli"),
    "cron": ("cron",),
}
```
- 网关日志独立输出到`gateway.log`，仅包含`gateway.*`命名空间的日志
- Agent日志与网关日志分离，便于问题定位
- 支持`hermes logs --component <name>`命令按组件过滤查看日志

### 3.4 敏感信息脱敏系统
`agent/redact.py` 提供多层级的敏感信息检测与替换，覆盖所有可能的泄露场景：
```python
# 已知API密钥前缀匹配
_PREFIX_PATTERNS = [
    r"sk-[A-Za-z0-9_-]{10,}",           # OpenAI / OpenRouter / Anthropic
    r"ghp_[A-Za-z0-9]{10,}",            # GitHub PAT (classic)
    r"github_pat_[A-Za-z0-9_]{10,}",    # GitHub PAT (fine-grained)
    r"gho_[A-Za-z0-9]{10,}",            # GitHub OAuth
    r"xox[baprs]-[A-Za-z0-9-]{10,}",    # Slack tokens
    r"AIza[A-Za-z0-9_-]{30,}",          # Google API keys
    r"pplx-[A-Za-z0-9]{10,}",           # Perplexity
    r"fal_[A-Za-z0-9_-]{10,}",          # Fal.ai
    r"fc-[A-Za-z0-9]{10,}",             # Firecrawl
    r"bb_live_[A-Za-z0-9_-]{10,}",      # BrowserBase
    r"gAAAA[A-Za-z0-9_=-]{20,}",        # Codex encrypted tokens
    r"AKIA[A-Z0-9]{16}",                # AWS Access Key ID
    r"sk_live_[A-Za-z0-9]{10,}",        # Stripe secret key
    r"r8_[A-Za-z0-9]{10,}",             # Replicate API token
    r"npm_[A-Za-z0-9]{10,}",            # npm access token
    r"pypi-[A-Za-z0-9_-]{10,}",         # PyPI API token
    r"dop_v1_[A-Za-z0-9]{10,}",         # DigitalOcean PAT
    r"sk_[A-Za-z0-9_]{10,}",            # ElevenLabs TTS key
    r"tvly-[A-Za-z0-9]{10,}",           # Tavily search key
    r"exa_[A-Za-z0-9]{10,}",            # Exa search key
    r"gsk_[A-Za-z0-9]{10,}",            # Groq Cloud key
    r"syt_[A-Za-z0-9]{10,}",            # Matrix access token
    r"retaindb_[A-Za-z0-9]{10,}",       # RetainDB API key
    r"hsk-[A-Za-z0-9]{10,}",            # Hindsight API key
    r"mem0_[A-Za-z0-9]{10,}",           # Mem0 Platform key
    r"brv_[A-Za-z0-9]{10,}",            # ByteRover API key
]

# 环境变量赋值匹配
_SECRET_ENV_NAMES = r"(?:API_?KEY|TOKEN|SECRET|PASSWORD|PASSWD|CREDENTIAL|AUTH)"
_ENV_ASSIGN_RE = re.compile(
    rf"([A-Z0-9_]{{0,50}}{_SECRET_ENV_NAMES}[A-Z0-9_]{{0,50}})\s*=\s*(['\"]?)(\S+)\2",
)

# JSON字段匹配
_JSON_KEY_NAMES = r"(?:api_?[Kk]ey|token|secret|password|access_token|refresh_token|auth_token|bearer|secret_value|raw_secret|secret_input|key_material)"
_JSON_FIELD_RE = re.compile(
    rf'("{_JSON_KEY_NAMES}")\s*:\s*"([^"]+)"',
    re.IGNORECASE,
)

# Authorization头匹配
_AUTH_HEADER_RE = re.compile(
    r"(Authorization:\s*Bearer\s+)(\S+)",
    re.IGNORECASE,
)

# Telegram bot token匹配
_TELEGRAM_RE = re.compile(
    r"(bot)?(\d{8,}):([-A-Za-z0-9_]{30,})",
)

# 私钥块匹配
_PRIVATE_KEY_RE = re.compile(
    r"-----BEGIN[A-Z ]*PRIVATE KEY-----[\s\S]*?-----END[A-Z ]*PRIVATE KEY-----"
)

# 数据库连接字符串匹配
_DB_CONNSTR_RE = re.compile(
    r"((?:postgres(?:ql)?|mysql|mongodb(?:\+srv)?|redis|amqp)://[^:]+:)([^@]+)(@)",
    re.IGNORECASE,
)

# JWT令牌匹配
_JWT_RE = re.compile(
    r"eyJ[A-Za-z0-9_-]{10,}"           # Header always starts with eyJ (base64 for "{")
    r"(?:\.[A-Za-z0-9_=-]{4,}){0,2}"   # Optional payload and signature
)

# Discord用户ID提及匹配
_DISCORD_MENTION_RE = re.compile(r"<@!?(\d{17,20})>")

# E.164电话号码匹配
_SIGNAL_PHONE_RE = re.compile(r"(\+[1-9]\d{6,14})(?![A-Za-z0-9])")
```

**脱敏策略：**
```python
def _mask_token(token: str) -> str:
    if len(token) < 18:
        return "***"
    return f"{token[:6]}...{token[-4:]}"
```
- 短令牌（<18位）：完全替换为`***`
- 长令牌：保留前6位和后4位，中间用`...`替换，便于调试识别
- 私钥块：替换为`[REDACTED PRIVATE KEY]`
- 数据库密码：替换为`***`，保留连接字符串其他部分
- 电话号码：保留前2位和后2位，中间用`****`替换
- Discord用户ID：替换为`***`，保护用户隐私

### 3.5 托管模式权限适配
```python
class _ManagedRotatingFileHandler(RotatingFileHandler):
    def __init__(self, *args, **kwargs):
        from hermes_cli.config import is_managed
        self._managed = is_managed()
        super().__init__(*args, **kwargs)

    def _chmod_if_managed(self):
        if self._managed:
            try:
                os.chmod(self.baseFilename, 0o660)
            except OSError:
                pass

    def _open(self):
        stream = super()._open()
        self._chmod_if_managed()
        return stream

    def doRollover(self):
        super().doRollover()
        self._chmod_if_managed()
```
- 在NixOS等托管部署环境中，日志文件权限自动设为`0o660`
- 确保网关进程和交互式CLI用户都能读写日志文件
- 继承标准`RotatingFileHandler`所有功能，完全兼容现有代码

### 3.6 第三方库噪音抑制
```python
_NOISY_LOGGERS = (
    "openai",
    "openai._base_client",
    "httpx",
    "httpcore",
    "asyncio",
    "hpack",
    "hpack.hpack",
    "grpc",
    "modal",
    "urllib3",
    "urllib3.connectionpool",
    "websockets",
    "charset_normalizer",
    "markdown_it",
)

# 在setup_logging中应用
for name in _NOISY_LOGGERS:
    logging.getLogger(name).setLevel(logging.WARNING)
```
- 自动将常见的第三方库日志级别设置为WARNING，减少无用日志输出
- 保持简洁的日志内容，突出业务相关信息
- 可通过`verbose`模式临时启用DEBUG级别日志

---

## 四、定时任务系统

定时任务系统采用轻量级设计，支持多种调度方式，多平台消息投递，内置防重放和容错机制。

### 4.1 架构设计
```
┌─────────────────────────────────────────────────────────────┐
│                     CRON SCHEDULER SYSTEM                    │
├─────────────────────────────────────────────────────────────┤
│  存储层                                                      │
│  • jobs.json: 任务元数据存储，JSON格式，原子写入             │
│  • output/<job_id>/<timestamp>.md: 任务运行结果存储          │
│  • 所有文件权限设为0o600，仅所有者可读                      │
├─────────────────────────────────────────────────────────────┤
│  调度层                                                      │
│  • 网关每60秒调用一次tick()，检查到期任务                   │
│  • 文件锁防止多进程重复执行，跨平台兼容（fcntl/msvcrt）      │
│  • 执行前推进next_run_at，崩溃后不会重放                     │
│  • 优雅窗口处理错过的任务，避免重启后任务风暴                 │
├─────────────────────────────────────────────────────────────┤
│  执行层                                                      │
│  • 支持前置脚本运行，动态注入任务上下文                      │
│  • 任务级超时控制，默认10分钟无活动自动终止                  │
│  • 工具调用活动检测，避免长时间挂起                          │
├─────────────────────────────────────────────────────────────┤
│  投递层                                                      │
│  • 支持18+个消息平台投递，优先使用网关长连接                 │
│  • 长连接不可用时回退到独立HTTP发送                         │
│  • 支持SILENT标记，无新内容时不发送消息                     │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 调度解析
```python
def parse_schedule(schedule: str) -> Dict[str, Any]:
    schedule = schedule.strip()
    
    # "every X" → 循环间隔
    if schedule_lower.startswith("every "):
        duration_str = schedule[6:].strip()
        minutes = parse_duration(duration_str)
        return {
            "kind": "interval",
            "minutes": minutes,
            "display": f"every {minutes}m"
        }
    
    # Cron表达式（5或6字段）
    parts = schedule.split()
    if len(parts) >= 5 and all(
        re.match(r'^[\d\*\-,/]+$', p) for p in parts[:5]
    ):
        croniter(schedule)  # 验证表达式有效性
        return {
            "kind": "cron",
            "expr": schedule,
            "display": schedule
        }
    
    # ISO时间戳 → 单次执行
    if 'T' in schedule or re.match(r'^\d{4}-\d{2}-\d{2}', schedule):
        dt = datetime.fromisoformat(schedule.replace('Z', '+00:00'))
        if dt.tzinfo is None:
            dt = dt.astimezone()  # 无时区信息视为本地时间
        return {
            "kind": "once",
            "run_at": dt.isoformat(),
            "display": f"once at {dt.strftime('%Y-%m-%d %H:%M')}"
        }
    
    # 持续时间（如30m, 2h, 1d）→ 单次延迟执行
    try:
        minutes = parse_duration(schedule)
        run_at = _hermes_now() + timedelta(minutes=minutes)
        return {
            "kind": "once",
            "run_at": run_at.isoformat(),
            "display": f"once in {original}"
        }
    except ValueError:
        pass
```
**支持的调度格式：**
| 输入示例 | 解析结果 |
|---------|---------|
| `"30m"`, `"2h"`, `"1d"` | 一次性任务，指定时间后执行 |
| `"every 30m"`, `"every 2h"` | 循环任务，按固定间隔执行 |
| `"0 9 * * *"` | Cron表达式，标准5字段格式 |
| `"2026-02-03T14:00"` | 一次性任务，指定时间点执行 |

### 4.3 防崩溃与防重放机制
```python
def advance_next_run(job_id: str) -> bool:
    jobs = load_jobs()
    for job in jobs:
        if job["id"] == job_id:
            kind = job.get("schedule", {}).get("kind")
            if kind not in ("cron", "interval"):
                return False
            now = _hermes_now().isoformat()
            new_next = compute_next_run(job["schedule"], now)
            if new_next and new_next != job.get("next_run_at"):
                job["next_run_at"] = new_next
                save_jobs(jobs)
                return True
            return False
```
**核心设计：**
1. **执行前推进**：周期性任务在执行前先将`next_run_at`推进到下一次时间
2. **原子更新**：任务状态更新使用原子写入，避免部分写入
3. **崩溃安全**：即使进程在执行过程中崩溃，下次重启也不会重复执行
4. **一次性任务例外**：一次性任务不提前推进，允许重启后重试
5. **调度语义**：从"至少一次"转变为"最多一次"，避免重复执行

### 4.4 优雅窗口与错过任务处理
```python
def _compute_grace_seconds(schedule: dict) -> int:
    MIN_GRACE = 120
    MAX_GRACE = 7200  # 2小时

    kind = schedule.get("kind")
    if kind == "interval":
        period_seconds = schedule.get("minutes", 1) * 60
        grace = period_seconds // 2
        return max(MIN_GRACE, min(grace, MAX_GRACE))
    
    if kind == "cron" and HAS_CRONITER:
        try:
            now = _hermes_now()
            cron = croniter(schedule["expr"], now)
            first = cron.get_next(datetime)
            second = cron.get_next(datetime)
            period_seconds = int((second - first).total_seconds())
            grace = period_seconds // 2
            return max(MIN_GRACE, min(grace, MAX_GRACE))
        except Exception:
            pass
    
    return MIN_GRACE
```
**策略：**
- 日常任务：最多2小时catch-up窗口
- 每小时任务：最多30分钟catch-up窗口
- 短间隔任务（<4分钟）：固定120秒窗口
- 超过窗口的错过任务直接跳过，避免重启后任务风暴
- 窗口大小动态计算为周期的一半，在120秒到2小时之间自动调整

### 4.5 任务执行与超时控制
```python
# 活动检测超时逻辑
_cron_timeout = float(os.getenv("HERMES_CRON_TIMEOUT", 600))
_cron_inactivity_limit = _cron_timeout if _cron_timeout > 0 else None
_POLL_INTERVAL = 5.0

_cron_pool = concurrent.futures.ThreadPoolExecutor(max_workers=1)
_cron_context = contextvars.copy_context()
_cron_future = _cron_pool.submit(_cron_context.run, agent.run_conversation, prompt)
_inactivity_timeout = False

try:
    if _cron_inactivity_limit is None:
        result = _cron_future.result()
    else:
        result = None
        while True:
            done, _ = concurrent.futures.wait(
                {_cron_future}, timeout=_POLL_INTERVAL,
            )
            if done:
                result = _cron_future.result()
                break
            # 检查Agent活动状态
            _idle_secs = 0.0
            if hasattr(agent, "get_activity_summary"):
                try:
                    _act = agent.get_activity_summary()
                    _idle_secs = _act.get("seconds_since_activity", 0.0)
                except Exception:
                    pass
            if _idle_secs >= _cron_inactivity_limit:
                _inactivity_timeout = True
                break
```
**超时机制：**
- **无活动超时**：默认10分钟无活动（工具调用、API响应、流数据）自动终止
- **活动跟踪**：Agent每次工具调用、API请求、接收流数据都会更新活动时间戳
- **优雅终止**：超时后调用`agent.interrupt()`通知任务终止，允许清理资源
- **诊断信息**：超时日志包含最后活动描述、当前运行工具、迭代进度等调试信息

### 4.6 多平台投递系统
```python
def _deliver_result(job: dict, content: str, adapters=None, loop=None) -> Optional[str]:
    target = _resolve_delivery_target(job)
    if not target:
        if job.get("deliver", "local") != "local":
            msg = f"no delivery target resolved for deliver={job.get('deliver', 'local')}"
            return msg
        return None

    platform_name = target["platform"]
    chat_id = target["chat_id"]
    thread_id = target.get("thread_id")

    # 优先使用网关长连接适配器（支持E2EE加密）
    runtime_adapter = (adapters or {}).get(platform)
    if runtime_adapter is not None and loop is not None and getattr(loop, "is_running", lambda: False)():
        send_metadata = {"thread_id": thread_id} if thread_id else None
        try:
            text_to_send = cleaned_delivery_content.strip()
            adapter_ok = True
            if text_to_send:
                future = asyncio.run_coroutine_threadsafe(
                    runtime_adapter.send(chat_id, text_to_send, metadata=send_metadata),
                    loop,
                )
                send_result = future.result(timeout=60)
                if send_result and not getattr(send_result, "success", True):
                    adapter_ok = False
            
            # 发送媒体附件
            if adapter_ok and media_files:
                _send_media_via_adapter(runtime_adapter, chat_id, media_files, send_metadata, loop, job)
            
            if adapter_ok:
                return None
        except Exception as e:
            pass

    # 回退到独立HTTP发送
    coro = _send_to_platform(platform, pconfig, chat_id, cleaned_delivery_content, thread_id=thread_id, media_files=media_files)
    try:
        result = asyncio.run(coro)
    except RuntimeError:
        # 已有运行事件循环时，在新线程中执行
        coro.close()
        import concurrent.futures
        with concurrent.futures.ThreadPoolExecutor(max_workers=1) as pool:
            future = pool.submit(asyncio.run, _send_to_platform(...))
            result = future.result(timeout=30)
```
**投递特性：**
- **双模式投递**：优先使用网关现有长连接，不重新建立连接，支持E2EE加密房间
- **媒体附件支持**：自动提取消息中的`MEDIA:`标签，作为原生平台附件发送
- **线程安全**：通过`asyncio.run_coroutine_threadsafe`跨线程调用异步方法
- **优雅回退**：长连接不可用时自动回退到独立HTTP发送，保证投递可靠性
- **静默模式**：当Agent返回内容包含`[SILENT]`标记时，跳过投递，仅保存结果本地

---

## 五、批处理系统

批处理系统专为大规模Agent运行设计，支持并行处理、Checkpoint恢复、工具统计标准化，是模型训练数据生成、评估基准测试的核心基础设施。

### 5.1 架构设计
```
┌─────────────────────────────────────────────────────────────┐
│                     BATCH PROCESSING SYSTEM                  │
├─────────────────────────────────────────────────────────────┤
│  核心特性                                                    │
│  • 并行执行：multiprocessing.Pool 多进程并行，避免GIL限制   │
│  • 粒度控制：并行粒度为Batch，而非单个Prompt，减少竞争       │
│  • Checkpoint：原子写入，增量保存，支持断点续跑             │
│  • 工具统计：统一Schema，支持HuggingFace datasets格式       │
│  • 分布采样：支持工具集分布采样，生成多样化训练数据         │
│  • 容器支持：每个任务可指定独立容器镜像，环境隔离           │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 并行执行机制
```python
def _process_batch_worker(args: Tuple) -> Dict[str, Any]:
    batch_num, batch_data, output_dir, completed_prompts_set, config = args
    
    # 过滤已完成的Prompt
    prompts_to_process = [
        (idx, data) for idx, data in batch_data
        if idx not in completed_prompts_set
    ]
    
    # 顺序处理Batch内的Prompt
    for prompt_index, prompt_data in prompts_to_process:
        # 每个Prompt独立Agent实例，避免状态污染
        selected_toolsets = sample_toolsets_from_distribution(config["distribution"])
        agent = AIAgent(
            base_url=config.get("base_url"),
            api_key=config.get("api_key"),
            model=config["model"],
            max_iterations=config["max_iterations"],
            enabled_toolsets=selected_toolsets,
            save_trajectories=False,
            verbose_logging=config.get("verbose", False),
            ephemeral_system_prompt=config.get("ephemeral_system_prompt"),
            skip_context_files=True,  # 避免加载用户环境的上下文文件
            skip_memory=True,         # 禁用持久化内存，避免交叉污染
        )
        result = agent.run_conversation(prompt, task_id=task_id)
```
**设计决策：**
- **进程级并行**：使用多进程而非多线程，避免Python GIL限制
- **Batch级并行粒度**：并行单位为整个Batch，而非单个Prompt，减少资源竞争
- **每个Prompt独立Agent实例**：确保会话完全隔离，无状态泄漏
- **Skip上下文文件和内存**：批处理不加载用户环境的自定义配置，确保环境一致性

### 5.3 容器镜像支持
```python
# 数据集行级容器镜像覆盖
container_image = prompt_data.get("image") or prompt_data.get("docker_image")
if container_image:
    env_type = os.getenv("TERMINAL_ENV", "local")
    if env_type == "docker":
        # 本地Docker环境下验证镜像存在，不存在则自动拉取
        import subprocess as _sp
        try:
            probe = _sp.run(
                ["docker", "image", "inspect", container_image],
                capture_output=True, timeout=10,
            )
            if probe.returncode != 0:
                pull = _sp.run(
                    ["docker", "pull", container_image],
                    capture_output=True, text=True, timeout=600,
                )
                if pull.returncode != 0:
                    return {
                        "success": False,
                        "prompt_index": prompt_index,
                        "error": f"Docker image not available: {container_image}\n{pull.stderr[:500]}",
                    }
        except FileNotFoundError:
            pass  # 非Docker环境，如Modal后端，跳过本地检查
    
    # 注册任务级环境覆盖
    from tools.terminal_tool import register_task_env_overrides
    overrides = {
        "docker_image": container_image,
        "modal_image": container_image,
        "singularity_image": f"docker://{container_image}",
        "daytona_image": container_image,
    }
    if prompt_data.get("cwd"):
        overrides["cwd"] = prompt_data.get("cwd")
    register_task_env_overrides(task_id, overrides)
```
**特性：**
- **数据集行级指定镜像**：每个Prompt可以指定独立的容器镜像
- **自动镜像拉取**：本地Docker环境下自动拉取不存在的镜像
- **多后端支持**：Docker、Modal、Singularity、Daytona等环境自动适配
- **工作目录指定**：支持自定义任务工作目录

### 5.4 Checkpoint 机制
```python
def _save_checkpoint(self, checkpoint_data: Dict[str, Any], lock: Optional[Lock] = None):
    checkpoint_data["last_updated"] = datetime.now().isoformat()
    from utils import atomic_json_write
    if lock:
        with lock:
            atomic_json_write(self.checkpoint_file, checkpoint_data)
    else:
        atomic_json_write(self.checkpoint_file, checkpoint_data)

# 增量更新Checkpoint
for result in pool.imap_unordered(_process_batch_worker, tasks):
    batch_num = result.get('batch_num')
    completed = result.get('completed_prompts', []) or []
    completed_prompts_set.update(completed)
    
    checkpoint_data.setdefault('batch_stats', {})[str(batch_num)] = {
        'processed': result.get('processed', 0),
        'skipped': result.get('skipped', 0),
        'discarded_no_reasoning': result.get('discarded_no_reasoning', 0),
    }
    
    checkpoint_data['completed_prompts'] = sorted(completed_prompts_set)
    self._save_checkpoint(checkpoint_data, lock=checkpoint_lock)
```
**可靠性设计：**
- **原子写入**：使用临时文件+fsync+os.replace三步原子写入，避免Checkpoint损坏
- **增量更新**：每完成一个Batch就更新一次Checkpoint，减少崩溃丢失进度
- **线程安全**：多进程环境下使用Lock保护Checkpoint写入
- **内容级Resume**：支持通过Prompt内容哈希匹配已完成任务，不依赖索引顺序

### 5.5 工具统计标准化
```python
def _normalize_tool_stats(tool_stats: Dict[str, Dict[str, int]]) -> Dict[str, Dict[str, int]]:
    normalized = {}
    # 确保所有可能的工具都存在，保持Schema一致性
    for tool in ALL_POSSIBLE_TOOLS:
        if tool in tool_stats:
            normalized[tool] = tool_stats[tool].copy()
        else:
            normalized[tool] = DEFAULT_TOOL_STATS.copy()
    # 保留未定义的新工具，兼容未来扩展
    for tool, stats in tool_stats.items():
        if tool not in normalized:
            normalized[tool] = stats.copy()
    return normalized

def _normalize_tool_error_counts(tool_error_counts: Dict[str, int]) -> Dict[str, int]:
    normalized = {}
    for tool in ALL_POSSIBLE_TOOLS:
        normalized[tool] = tool_error_counts.get(tool, 0)
    for tool, count in tool_error_counts.items():
        if tool not in normalized:
            normalized[tool] = count
    return normalized
```
**标准化意义：**
- **HuggingFace datasets兼容**：所有样本Schema完全一致，无需后续处理
- **工具统计字段**：包含`count`（调用次数）、`success`（成功次数）、`failure`（失败次数）
- **工具错误统计**：直接映射工具名称到失败次数，便于分析工具可靠性
- **向前兼容**：新增工具自动包含在Schema中，旧数据集无需修改

### 5.6 无效样本过滤
```python
# 过滤无推理内容的样本
reasoning = result.get("reasoning_stats", {})
if not reasoning.get("has_any_reasoning", True):
    print(f"   🚫 Prompt {prompt_index} discarded (no reasoning in any turn)")
    discarded_no_reasoning += 1
    continue

# 过滤工具调用包含无效工具名称的样本（模型幻觉）
tool_stats = data.get('tool_stats', {})
invalid_tools = [k for k in tool_stats if k not in VALID_TOOLS]
if invalid_tools:
    filtered_entries += 1
    continue
```
**质量控制：**
- **无推理过滤**：丢弃所有Assistant回复都不包含推理过程的样本
- **无效工具过滤**：丢弃包含不存在工具名称的样本，避免模型幻觉污染数据集
- **损坏JSON过滤**：自动过滤解析失败的样本
- **统计报告**：过滤结果在最终报告中统计，便于评估数据集质量

---

### 5.7 基准测试环境目录

`environments/` 目录包含可插拔的基准测试和评估环境，支持多种后端运行 SWE-bench、TerminalBench、YC-Bench 等评测：

```
environments/
├── hermes_base_env.py          # 基础环境抽象类
├── agent_loop.py               # 通用 Agent 交互循环
├── agentic_opd_env.py          # Agentic OPD 环境
├── patches.py                  # 补丁工具
├── tool_context.py             # 工具调用上下文
├── web_research_env.py         # Web 研究环境
├── hermes_swe_env/             # SWE-bench 评估环境
├── benchmarks/
│   ├── terminalbench_2/        # TerminalBench v2 评测
│   ├── tblite/                  # TBLite 轻量级评测
│   └── yc_bench/               # YC-Bench 评测
├── terminal_test_env/          # 终端测试环境
└── tool_call_parsers/          # 多模型工具调用解析器
    ├── deepseek_v3_1_parser.py
    ├── glm47_parser.py
    ├── llama_parser.py
    ├── qwen3_coder_parser.py
    └── ... (12 种解析器)
```

**特点：**
- 每个环境有独立的 `default.yaml` 配置，通过 `run_eval.sh` 脚本运行
- 支持三种后端：本地（直接运行）、Docker（容器隔离）、Modal（云端分布式）
- `tool_call_parsers/` 提供多模型工具调用格式解析，避免对特定模型的硬编码依赖

---

## 六、轨迹压缩系统

轨迹压缩系统解决长对话Token超限问题，在保留上下文语义的前提下大幅减少Token使用，支持训练数据压缩和运行时上下文压缩两种场景。

### 6.1 压缩策略
```
┌─────────────────────────────────────────────────────────────┐
│                     COMPRESSION STRATEGY                     │
├─────────────────────────────────────────────────────────────┤
│  保护区域                                                    │
│  • 头部：系统提示、用户初始请求、首条Assistant回复、首次工具调用 │
│  • 尾部：最后N轮对话（默认4轮），保留最近上下文             │
├─────────────────────────────────────────────────────────────┤
│  压缩区域                                                    │
│  • 中间对话轮次：从头开始累积，直到达到需要节省的Token数     │
│  • 压缩为单条摘要消息：由LLM生成，保留关键动作和信息         │
│  • 剩余未压缩轮次保持完整，工具调用历史不变                 │
├─────────────────────────────────────────────────────────────┤
│  压缩保证                                                    │
│  • 压缩后总Token ≤ 目标Token数（默认15250）                  │
│  • 摘要长度目标750 Token，平衡信息密度和上下文大小           │
│  • 工具调用元数据完整保留，不影响后续工具使用               │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 核心实现
```python
def compress_trajectory(
    self, trajectory: List[Dict[str, str]]
) -> Tuple[List[Dict[str, str]], TrajectoryMetrics]:
    # 1. 统计Token分布
    turn_tokens = self.count_turn_tokens(trajectory)
    total_tokens = sum(turn_tokens)
    
    # 2. 无需压缩，直接返回
    if total_tokens <= self.config.target_max_tokens:
        metrics.skipped_under_target = True
        return trajectory, metrics
    
    # 3. 确定可压缩区域
    protected, compress_start, compress_end = self._find_protected_indices(trajectory)
    if compress_start >= compress_end:
        metrics.still_over_limit = True
        return trajectory, metrics
    
    # 4. 计算需要压缩的Token量
    tokens_to_save = total_tokens - self.config.target_max_tokens
    target_tokens_to_compress = tokens_to_save + self.config.summary_target_tokens
    
    # 5. 累积需要压缩的轮次
    accumulated_tokens = 0
    compress_until = compress_start
    for i in range(compress_start, compress_end):
        accumulated_tokens += turn_tokens[i]
        compress_until = i + 1
        if accumulated_tokens >= target_tokens_to_compress:
            break
    
    # 6. 压缩区域不足，压缩全部可压缩区域
    if accumulated_tokens < target_tokens_to_compress and compress_until < compress_end:
        compress_until = compress_end
        accumulated_tokens = sum(turn_tokens[compress_start:compress_end])
    
    # 7. 生成摘要
    content_to_summarize = self._extract_turn_content_for_summary(
        trajectory, compress_start, compress_until
    )
    summary = self._generate_summary(content_to_summarize, metrics)
    
    # 8. 构建压缩后的轨迹
    compressed = []
    # 添加头部轮次，系统提示追加压缩通知
    for i in range(compress_start):
        turn = trajectory[i].copy()
        if turn.get("from") == "system" and self.config.add_summary_notice:
            turn["value"] = turn["value"] + self.config.summary_notice_text
        compressed.append(turn)
    # 添加摘要
    compressed.append({
        "from": "human",
        "value": summary
    })
    # 添加尾部轮次
    for i in range(compress_until, len(trajectory)):
        compressed.append(trajectory[i].copy())
    
    return compressed, metrics
```
**压缩保证：**
- 无信息丢失风险：仅压缩中间轮次，首尾关键上下文完整保留
- Token节省可控：仅压缩到刚好满足目标Token数，最大化保留原始信息
- 摘要质量保证：使用专门的prompt引导模型提取关键动作和信息
- 运行时友好：摘要作为用户消息注入，无需修改Agent逻辑

### 6.3 摘要生成
```python
def _generate_summary(self, content: str, metrics: TrajectoryMetrics) -> str:
    prompt = f"""Summarize the following agent conversation turns concisely. This summary will replace these turns in the conversation history.

Write the summary from a neutral perspective describing what the assistant did and learned. Include:
1. What actions the assistant took (tool calls, searches, file operations)
2. Key information or results obtained
3. Important decisions or findings
4. Relevant data, file names, values, or outputs

Keep the summary factual and informative. Target approximately {self.config.summary_target_tokens} tokens.

---
TURNS TO SUMMARIZE:
{content}
---

Write only the summary, starting with "[CONTEXT SUMMARY]:" prefix."""

    for attempt in range(self.config.max_retries):
        try:
            metrics.summarization_api_calls += 1
            # 使用配置的模型生成摘要，支持OpenRouter等多平台
            response = self.client.chat.completions.create(
                model=self.config.summarization_model,
                messages=[{"role": "user", "content": prompt}],
                temperature=self.config.temperature,
                max_tokens=self.config.summary_target_tokens * 2,
            )
            summary = self._coerce_summary_content(response.choices[0].message.content)
            return self._ensure_summary_prefix(summary)
        except Exception as e:
            metrics.summarization_errors += 1
            if attempt < self.config.max_retries - 1:
                time.sleep(jittered_backoff(attempt + 1, base_delay=self.config.retry_delay, max_delay=30.0))
            else:
                # 降级方案，使用通用摘要
                return "[CONTEXT SUMMARY]: [Summary generation failed - previous turns contained tool calls and responses that have been compressed to save context space.]"
```
**摘要特性：**
- **结构化信息提取**：强制包含动作、结果、决策、关键数据四个维度
- **Token控制**：目标750 Token，生成时限制最大长度为目标的2倍
- **重试机制**：最多3次重试，指数退避，增加成功率
- **降级处理**：API调用失败时返回通用摘要，不中断处理流程

### 6.4 并行处理架构
```python
async def _process_directory_async(self, input_dir: Path, output_dir: Path):
    # 信号量控制并发API调用数，避免超限
    semaphore = asyncio.Semaphore(self.config.max_concurrent_requests)
    
    async def process_single(file_path: Path, entry_idx: int, entry: Dict, progress, main_task, status_task):
        nonlocal compressed_count, skipped_count, api_calls, in_flight, timeout_count
        
        async with semaphore:
            async with progress_lock:
                in_flight += 1
            
            try:
                # 单任务超时控制，默认300秒
                processed_entry, metrics = await asyncio.wait_for(
                    self.process_entry_async(entry),
                    timeout=self.config.per_trajectory_timeout
                )
                # 更新统计
                async with progress_lock:
                    self.aggregate_metrics.add_trajectory_metrics(metrics)
                    if metrics.was_compressed:
                        compressed_count += 1
                        api_calls += metrics.summarization_api_calls
                    if metrics.skipped_under_target:
                        skipped_count += 1
                    in_flight -= 1
                    progress.advance(main_task)
            except asyncio.TimeoutError:
                timeout_count += 1
                return None
    
    # 创建所有任务并执行
    tasks = [process_single(...) for ... in all_entries]
    await asyncio.gather(*tasks)
```
**性能优化：**
- **异步IO**：使用异步API调用，大幅提升IO密集型压缩任务的吞吐量
- **并发控制**：通过Semaphore限制同时进行的API调用数，避免超过服务商配额
- **任务级超时**：每个任务独立超时控制，避免单个失败任务阻塞整个流程
- **实时进度**：Rich进度条实时显示压缩进度、成功数、跳过数、API调用数等指标

---

## 七、Docker 部署

项目提供官方Docker镜像，开箱即用，安全优化，支持多架构。

### 7.1 镜像设计

真实Dockerfile采用多阶段构建（multi-stage build），从外部来源复制 `uv` 和 `gosu` 工具，基础镜像为 `debian:13.4`。

```dockerfile
# ── 多阶段构建：从独立镜像复制工具 ──
FROM ghcr.io/astral-sh/uv:0.11.6-python3.13-trixie AS uv_source
FROM tianon/gosu:1.19-trixie AS gosu_source
FROM debian:13.4

ENV PYTHONUNBUFFERED=1
ENV PLAYWRIGHT_BROWSERS_PATH=/opt/hermes/.playwright

# 系统依赖（build-essential, nodejs, npm, python3, ripgrep 等）
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        build-essential nodejs npm python3 ripgrep ffmpeg gcc python3-dev libffi-dev procps git && \
    rm -rf /var/lib/apt/lists/*

# 创建非root用户（UID 10000，可在运行时通过 HERMES_UID 覆盖）
RUN useradd -u 10000 -m -d /opt/data hermes

# 从多阶段构建复制工具
COPY --chmod=0755 --from=gosu_source /gosu /usr/local/bin/
COPY --chmod=0755 --from=uv_source /usr/local/bin/uv /usr/local/bin/uvx /usr/local/bin/

COPY . /opt/hermes
WORKDIR /opt/hermes

# 以root安装Node依赖和Playwright浏览器
RUN npm install --prefer-offline --no-audit && \
    npx playwright install --with-deps chromium --only-shell && \
    cd /opt/hermes/scripts/whatsapp-bridge && \
    npm install --prefer-offline --no-audit && \
    npm cache clean --force

# 切换所有权给hermes用户，然后安装Python依赖
RUN chown -R hermes:hermes /opt/hermes
USER hermes

# 使用 uv 而非 pip 安装Python依赖
RUN uv venv && \
    uv pip install --no-cache-dir -e ".[all]"

USER root
RUN chmod +x /opt/hermes/docker/entrypoint.sh

ENV HERMES_HOME=/opt/data
VOLUME [ "/opt/data" ]
ENTRYPOINT [ "/opt/hermes/docker/entrypoint.sh" ]
```

**设计要点：**
- **多阶段构建**：从 `ghcr.io/astral-sh/uv` 和 `tianon/gosu` 复制工具，避免在镜像内编译
- **uv 包管理器**：使用 `uv venv` + `uv pip install`（而非 pip），速度更快
- **Playwright 浏览器路径**：通过 `PLAYWRIGHT_BROWSERS_PATH` 存储在镜像层而非 volume 上，避免运行时覆盖
- **gosu 权限降级**：entrypoint 以 root 启动，通过 `gosu` 降级到 hermes 用户（UID/GID 可通过环境变量配置）
- **Non-root 用户 UID 10000**：避免与宿主机常见 UID 冲突
- **entrypoint.sh 首次引导**：自动创建 `.env`、`config.yaml`、`SOUL.md`（从 `.env.example`、`cli-config.yaml.example`、`docker/SOUL.md` 复制）

### 7.2 entrypoint.sh 引导流程

```bash
#!/bin/bash
# 1. 权限降级：如以 root 启动，通过 gosu 切换到 hermes 用户
#    - 支持 HERMES_UID / HERMES_GID 环境变量覆盖用户/组ID
#    - 自动修复 volume 权限（chown）
#    - rootless Podman 下优雅跳过 chown 失败
# 2. 激活虚拟环境：source /opt/hermes/.venv/bin/activate
# 3. 创建目录结构：cron, sessions, logs, hooks, memories, skills, skins, plans, workspace, home
# 4. 首次引导：复制 .env.example → .env, cli-config.yaml.example → config.yaml, docker/SOUL.md → SOUL.md
# 5. 同步 bundled skills：python3 tools/skills_sync.py
# 6. exec hermes "$@"
```

### 7.3 Registry 与架构

- **Registry**：`nousresearch/hermes-agent:latest`（Docker Hub）
- **架构支持**：`linux/amd64` + `linux/arm64` 多架构镜像
- **CI 构建**：通过 GitHub Actions `docker-publish.yml` 自动构建和推送
- **Smoke test**：CI 中构建后运行 `docker run --rm --entrypoint /opt/hermes/docker/entrypoint.sh nousresearch/hermes-agent:test --help` 验证镜像可用

### 7.4 运行方式
```bash
# 基本运行
docker run -it --rm -v $(pwd)/hermes-data:/opt/data nousresearch/hermes-agent:latest

# 传递环境变量
docker run -it --rm \
  -v $(pwd)/hermes-data:/opt/data \
  -e OPENAI_API_KEY=sk-xxx \
  -e TELEGRAM_BOT_TOKEN=xxx \
  nousresearch/hermes-agent:latest

# 自定义 UID/GID 匹配宿主机文件权限
docker run -it --rm \
  -v $(pwd)/hermes-data:/opt/data \
  -e HERMES_UID=$(id -u) -e HERMES_GID=$(id -g) \
  nousresearch/hermes-agent:latest
```

---

## 八、依赖管理

项目采用现代化Python依赖管理，基于PEP 621标准，使用pyproject.toml统一配置。

### 8.1 pyproject.toml 结构

```toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-agent"
version = "0.9.0"
description = "The self-improving AI agent — creates skills from experience, improves them during use, and runs anywhere"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Nous Research" }]
license = { text = "MIT" }
dependencies = [
  # Core — pinned to known-good ranges to limit supply chain attack surface
  "openai>=2.21.0,<3",
  "anthropic>=0.39.0,<1",
  "python-dotenv>=1.2.1,<2",
  "fire>=0.7.1,<1",
  "httpx[socks]>=0.28.1,<1",
  "rich>=14.3.3,<15",
  "tenacity>=9.1.4,<10",
  "pyyaml>=6.0.2,<7",
  "requests>=2.33.0,<3",
  "jinja2>=3.1.5,<4",
  "pydantic>=2.12.5,<3",
  "prompt_toolkit>=3.0.52,<4",
  # Tools
  "exa-py>=2.9.0,<3",
  "firecrawl-py>=4.16.0,<5",
  "parallel-web>=0.4.2,<1",
  "fal-client>=0.13.1,<1",
  # Text-to-speech (Edge TTS is free, no API key needed)
  "edge-tts>=7.2.7,<8",
  "PyJWT[crypto]>=2.12.0,<3",
]

[project.optional-dependencies]
modal = ["modal>=1.0.0,<2"]
daytona = ["daytona>=0.148.0,<1"]
dev = ["debugpy>=1.8.0,<2", "pytest>=9.0.2,<10", "pytest-asyncio>=1.3.0,<2", "pytest-xdist>=3.0,<4", "mcp>=1.2.0,<2"]
messaging = ["python-telegram-bot[webhooks]>=22.6,<23", "discord.py[voice]>=2.7.1,<3", "aiohttp>=3.13.3,<4", "slack-bolt>=1.18.0,<2", "slack-sdk>=3.27.0,<4"]
cron = ["croniter>=6.0.0,<7"]
slack = ["slack-bolt>=1.18.0,<2", "slack-sdk>=3.27.0,<4"]
matrix = ["mautrix[encryption]>=0.20,<1", "Markdown>=3.6,<4", "aiosqlite>=0.20", "asyncpg>=0.29"]
cli = ["simple-term-menu>=1.0,<2"]
tts-premium = ["elevenlabs>=1.0,<2"]
voice = ["faster-whisper>=1.0.0,<2", "sounddevice>=0.4.6,<1", "numpy>=1.24.0,<3"]
pty = ["ptyprocess>=0.7.0,<1; sys_platform != 'win32'", "pywinpty>=2.0.0,<3; sys_platform == 'win32'"]
honcho = ["honcho-ai>=2.0.1,<3"]
mcp = ["mcp>=1.2.0,<2"]
homeassistant = ["aiohttp>=3.9.0,<4"]
sms = ["aiohttp>=3.9.0,<4"]
acp = ["agent-client-protocol>=0.9.0,<1.0"]
mistral = ["mistralai>=2.3.0,<3"]
bedrock = ["boto3>=1.35.0,<2"]
termux = ["python-telegram-bot[webhooks]>=22.6,<23", "hermes-agent[cron]", "hermes-agent[cli]", "hermes-agent[pty]", "hermes-agent[mcp]", "hermes-agent[honcho]", "hermes-agent[acp]"]
dingtalk = ["dingtalk-stream>=0.1.0,<1"]
feishu = ["lark-oapi>=1.5.3,<2"]
web = ["fastapi>=0.104.0,<1", "uvicorn[standard]>=0.24.0,<1"]
rl = ["atroposlib @ git+...", "tinker @ git+...", "fastapi>=0.104.0,<1", "uvicorn[standard]>=0.24.0,<1", "wandb>=0.15.0,<1"]
all = ["hermes-agent[modal,daytona,messaging,cron,cli,dev,tts-premium,slack,pty,honcho,mcp,homeassistant,sms,acp,voice,dingtalk,feishu,mistral,bedrock,web]"]

[project.scripts]
hermes = "hermes_cli.main:main"
hermes-agent = "run_agent:main"
hermes-acp = "acp_adapter.entry:main"

[tool.setuptools]
py-modules = ["run_agent", "model_tools", "toolsets", "batch_runner", "trajectory_compressor", "toolset_distributions", "cli", "hermes_constants", "hermes_state", "hermes_time", "hermes_logging", "rl_cli", "utils"]

[tool.setuptools.packages.find]
include = ["agent", "tools", "tools.*", "hermes_cli", "gateway", "gateway.*", "cron", "acp_adapter", "plugins", "plugins.*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: marks tests requiring external services (API keys, Modal, etc.)"]
addopts = "-m 'not integration' -n auto"
```

**设计特点：**
- **版本 0.9.0**（预发布阶段），作者为 **Nous Research**（非"LYK"）
- **核心依赖精简**：不含 `torch`、`transformers`、`schedule`、`croniter`——这些分别属于 `voice`、`cron` 可选组
- **20+ 可选依赖组**：按功能细分，用户可按需安装（`[messaging]`、`[voice]`、`[cron]`、`[modal]` 等）
- **`[all]` 聚合**：有平台条件（如 `[matrix]` 仅在 Linux 上安装，因 python-olm 在 macOS 上被上游破坏）
- **三条 CLI 入口**：`hermes`（主CLI）、`hermes-agent`（直接运行agent）、`hermes-acp`（ACP协议适配器）
- **pytest 配置**：默认跳过集成测试（`-m 'not integration'`），使用 `-n auto` 并行运行

---

## 九、设计模式

项目广泛使用经典设计模式，平衡代码复用性、可扩展性和可维护性。

### 9.1 单例模式
```python
# 路径解析模块级缓存
_hermes_home = None
def get_hermes_home() -> Path:
    global _hermes_home
    if _hermes_home is None:
        _hermes_home = Path(os.getenv("HERMES_HOME", Path.home() / ".hermes"))
    return _hermes_home

# 环境检测结果缓存
_wsl_detected = None
_container_detected = None
```
- 全局唯一实例，避免重复计算
- 线程安全，无状态副作用
- 提升性能，避免重复IO操作

### 9.2 工厂模式
```python
def resolve_runtime_provider(requested: str = None) -> Dict[str, Any]:
    """根据环境变量和配置自动解析运行时提供商"""
    if requested:
        provider = requested.lower()
    else:
        # 自动检测优先级：OPENAI_API_KEY → ANTHROPIC_API_KEY → OPENROUTER_API_KEY
        if os.getenv("OPENAI_API_KEY"):
            provider = "openai"
        elif os.getenv("ANTHROPIC_API_KEY"):
            provider = "anthropic"
        elif os.getenv("OPENROUTER_API_KEY"):
            provider = "openrouter"
        else:
            raise ValueError("No API key found, please set one of OPENAI_API_KEY, ANTHROPIC_API_KEY, OPENROUTER_API_KEY")
    
    # 构建提供商配置
    if provider == "openai":
        return {
            "provider": "openai",
            "base_url": os.getenv("OPENAI_BASE_URL", "https://api.openai.com/v1"),
            "api_key": os.getenv("OPENAI_API_KEY"),
        }
    elif provider == "anthropic":
        return {
            "provider": "anthropic",
            "base_url": os.getenv("ANTHROPIC_BASE_URL", "https://api.anthropic.com"),
            "api_key": os.getenv("ANTHROPIC_API_KEY"),
        }
    # 更多提供商...
```
- 封装对象创建逻辑，客户端无需了解细节
- 配置驱动，支持动态切换实现
- 新增提供商只需添加工厂分支，无需修改调用代码

### 9.3 Collector 模式（PluginCollector）

项目中不存在建造者模式。真实的代码使用了 **Collector** 模式，在 `plugins/memory/__init__.py` 中实现。该模式用于从插件模块中收集注册信息：

```python
class _ProviderCollector:
    """Fake plugin context that captures register_memory_provider calls."""

    def __init__(self):
        self.provider = None

    def register_memory_provider(self, provider):
        self.provider = provider

    # 其他注册方法为no-op
    def register_tool(self, *args, **kwargs):
        pass

    def register_hook(self, *args, **kwargs):
        pass

    def register_cli_command(self, *args, **kwargs):
        pass
```

**使用方式：**
```python
# 在 _load_provider_from_dir 中
if hasattr(mod, "register"):
    collector = _ProviderCollector()
    try:
        mod.register(collector)  # 插件调用 register_memory_provider(provider)
        if collector.provider:
            return collector.provider
    except Exception as e:
        logger.debug("register() failed for %s: %s", name, e)
```

**设计特点：**
- **最小化上下文暴露**：插件只能调用预定义方法，不能访问内部状态
- **优雅降级**：`register()` 失败时回退到类名扫描（查找 MemoryProvider 子类）
- **双重发现机制**：优先使用 `register(ctx)` 契约，回退到 `isinstance` 类型检查
- 对比建造者模式：项目通过 `AIAgent.__init__()` 直接接收所有参数，不使用链式构造器

### 9.4 Agent 对话主循环

项目中不存在模板方法模式的 `_should_continue()`、`_add_tool_results_to_context()`、`_build_result()` 等方法。真实的 `run_conversation()` 方法（`run_agent.py`）是一个完整的对话循环，不是模板方法骨架。其核心流程为：

```
run_conversation(user_message, conversation_history=None, task_id=None, stream_callback=None)
│
├─ 1. 安装安全 stdio guard（防止 broken pipe 崩溃）
├─ 2. 设置会话日志上下文（session_id → thread local）
├─ 3. 恢复主运行时（如上一轮触发了 fallback）
├─ 4. 清理 Unicode surrogate 字符
├─ 5. 重置重试计数器（_invalid_tool_retries, _invalid_json_retries 等）
├─ 6. 死连接清理（检测并清理僵尸 TCP 连接）
├─ 7. Replay 压缩警告（通过 status_callback 发送给 gateway 平台）
├─ 8. 初始化 IterationBudget(max_iterations)
├─ 9. 从历史恢复 todo_store 状态
├─ 10. 内存/skill nudge 逻辑（turn-based 检查）
│
├─ 11. 构建/复用系统提示
│    ├─ 如果 _cached_system_prompt 存在 → 复用（保持 Anthropic prefix cache）
│    ├─ 如果 session DB 中有存储的 system_prompt → 载入
│    └─ 否则 → _build_system_prompt() 从头构建
│        └─ 触发 on_session_start hook（仅新会话）
│
├─ 12. 主循环
│    ├─ 调用LLM（流式或非流式，支持中断）
│    ├─ 处理工具调用
│    ├─ 错误处理（JSON 解析失败、SIGBUS 重试、fallback 运行时切换）
│    ├─ 压缩触发（token 超限时 context_engine.compress()）
│    └─ check_iteration_limit() 检查
│
├─ 13. 达到最大迭代 → 生成摘要（_get_summary_response）
├─ 14. 最终响应处理（清理 <think> 标签、工具调用重映射）
├─ 15. 内存 flush（_flush_session_to_db、_flush_memory）
├─ 16. on_session_end / on_session_finalize hooks
└─ 17. 返回完整结果字典（final_response + message_history + token stats）
```

**设计特点：**
- **非模板方法**：整个流程是单体方法，不通过子类重写扩展
- **前缀缓存感知**：系统提示复用策略是为 Anthropic prompt caching 优化的
- **全面错误恢复**：JSON解析失败、空内容、死连接、SIGBUS、fallback运行时切换均有处理
- **Hook 注入点**：`on_session_start`、`on_session_end`、`on_session_finalize` 为插件提供扩展点

### 9.5 检查点模式
```python
def save_checkpoint(self, batch_num: int, completed_prompts: List[int]):
    """原子保存检查点"""
    checkpoint = {
        "batch_num": batch_num,
        "completed_prompts": completed_prompts,
        "timestamp": datetime.now().isoformat(),
        "version": __version__
    }
    # 原子写入
    temp_file = self.checkpoint_path.with_suffix(".tmp")
    with open(temp_file, "w") as f:
        json.dump(checkpoint, f, indent=2)
        f.flush()
        os.fsync(f.fileno())
    os.replace(temp_file, self.checkpoint_path)
```
- 原子操作，保证检查点一致性
- 增量保存，避免全量重写
- 崩溃恢复，支持断点续跑

---

## 九点五、插件系统

项目通过 `hermes_cli/plugins.py` 实现了完整的插件系统，支持从三个来源发现和加载插件。

### 9.5.1 三大发现来源

```python
def discover_and_load(self) -> None:
    manifests: List[PluginManifest] = []

    # 1. 用户插件 (~/.hermes/plugins/<name>/)
    user_dir = get_hermes_home() / "plugins"
    manifests.extend(self._scan_directory(user_dir, source="user"))

    # 2. 项目插件 (./.hermes/plugins/<name>/) — 通过 HERMES_ENABLE_PROJECT_PLUGINS 环境变量启用
    if _env_enabled("HERMES_ENABLE_PROJECT_PLUGINS"):
        project_dir = Path.cwd() / ".hermes" / "plugins"
        manifests.extend(self._scan_directory(project_dir, source="project"))

    # 3. Pip 插件 — 通过 hermes_agent.plugins entry-point 组发现
    manifests.extend(self._scan_entry_points())
```

### 9.5.2 plugin.yaml 清单

每个目录插件必须包含 `plugin.yaml`（也接受 `plugin.yml`）和 `__init__.py`（含 `register(ctx)` 函数）：

```yaml
name: my-plugin
version: 1.0.0
description: A sample plugin
author: dev
requires_env:
  - MY_PLUGIN_API_KEY
provides_tools:
  - my_custom_tool
provides_hooks:
  - pre_tool_call
  - on_session_start
```

### 9.5.3 register(ctx) 契约与 10 种 Hook

插件通过 `register(ctx)` 接收 `PluginContext` 对象，可以注册：

```python
# PluginContext 提供的注册方法：
ctx.register_tool(name, toolset, schema, handler, check_fn, ...)  # 注册工具到全局registry
ctx.register_hook(hook_name, callback)                              # 注册生命周期回调
ctx.register_cli_command(name, help, setup_fn, handler_fn)          # 注册CLI子命令
ctx.register_context_engine(engine)                                 # 替换内置ContextCompressor
ctx.register_skill(name, path, description)                         # 注册只读skill（命名空间隔离）
ctx.inject_message(content, role)                                   # 向活跃对话注入消息
```

**10 种有效 Hook：**

| Hook | 触发时机 |
|------|---------|
| `pre_tool_call` | 工具调用前（可返回 `{"action": "block", "message": "..."}` 阻止调用） |
| `post_tool_call` | 工具调用后 |
| `pre_llm_call` | LLM API 调用前（可返回 `{"context": "injected text"}` 注入上下文） |
| `post_llm_call` | LLM API 调用后 |
| `pre_api_request` | 任何 API 请求前 |
| `post_api_request` | 任何 API 请求后 |
| `on_session_start` | 新会话创建时（仅首次，断线重连不触发） |
| `on_session_end` | 会话结束时（保存后触发） |
| `on_session_finalize` | 会话最终化时 |
| `on_session_reset` | 会话重置时 |

### 9.5.4 Hook 注入与 Prompt 缓存

`pre_llm_call` hook 可以注入上下文到当前轮次的用户消息中：

```python
# 回调可以返回：
{"context": "recalled memory text..."}   # dict 形式
"recalled memory text..."               # 纯字符串形式，等价
```

**关键设计：注入的上下文总是追加到 user 消息中，从不修改 system prompt。** 这确保了 Anthropic prompt cache 前缀不受影响——system prompt 在各轮次间保持完全一致，缓存 token 得以复用。

### 9.5.5 PluginManager 架构

```python
class PluginManager:
    """中央管理器，发现、加载并调用所有插件。"""
    
    def __init__(self):
        self._plugins: Dict[str, LoadedPlugin] = {}     # 已加载的插件
        self._hooks: Dict[str, List[Callable]] = {}     # 已注册的回调
        self._plugin_tool_names: Set[str] = set()       # 插件提供的工具名
        self._cli_commands: Dict[str, dict] = {}         # 插件注册的CLI命令
        self._context_engine = None                      # 插件注册的上下文引擎
        self._plugin_skills: Dict[str, Dict] = {}        # 插件注册的skills
    
    def invoke_hook(self, hook_name: str, **kwargs) -> List[Any]:
        """调用所有已注册的回调，每个回调被独立 try/except 包裹"""
    
    def discover_and_load(self) -> None:
        """幂等操作——发现并加载所有插件"""
```

- **Singleton 访问**：`get_plugin_manager()` 返回全局唯一实例
- **容错设计**：每个 hook 回调独立 try/except，一个插件崩溃不影响其他插件
- **幂等发现**：`discover_and_load()` 可多次调用，只有第一次执行
- **用户可禁用**：通过 `config.yaml` 的 `plugins.disabled` 列表禁用特定插件

---

## 九点六、SQLite 状态存储

`hermes_state.py` 提供了基于 SQLite 的持久化会话存储，替代了早期的每会话 JSONL 文件方式。

### 9.6.1 架构设计

```python
class SessionDB:
    """SQLite-backed session storage with FTS5 search."""
    
    def __init__(self, db_path=None):
        self.db_path = db_path or get_hermes_home() / "state.db"
        self._lock = threading.Lock()
        self._conn = sqlite3.connect(
            str(self.db_path),
            check_same_thread=False,
            timeout=1.0,        # 短超时——应用层重试处理争用
            isolation_level=None,  # 手动管理事务
        )
        self._conn.row_factory = sqlite3.Row
        self._conn.execute("PRAGMA journal_mode=WAL")
        self._conn.execute("PRAGMA foreign_keys=ON")
```

### 9.6.2 WAL 模式与写争用缓解

- **WAL 模式**：支持并发读取 + 单一写入者（gateway 多平台场景）
- **写重试策略**：最多 15 次重试，每次 20-150ms 随机 jitter
- **BEGIN IMMEDIATE**：在事务开始时获取 WAL 写锁（而非提交时），避免锁争用放大
- **混行问题处理**：SQLite 内置忙等待使用确定性退避，在高并发下会导致 convoy effect。项目使用随机 jitter 自然错开竞争写入者
- **PASSIVE WAL checkpoint**：每 50 次成功写入触发一次，防止 WAL 文件无限增长

```python
def _execute_write(self, fn: Callable) -> T:
    for attempt in range(self._WRITE_MAX_RETRIES):  # 15 次
        try:
            with self._lock:
                self._conn.execute("BEGIN IMMEDIATE")
                result = fn(self._conn)
                self._conn.commit()
            # 成功——定期触发 checkpoint
            self._write_count += 1
            if self._write_count % self._CHECKPOINT_EVERY_N_WRITES == 0:
                self._try_wal_checkpoint()
            return result
        except sqlite3.OperationalError as exc:
            if "locked" in str(exc).lower():
                time.sleep(random.uniform(0.020, 0.150))  # jitter
                continue
            raise
```

### 9.6.3 Schema 设计

**sessions 表**：存储会话元数据，包括来源平台标记（`source`）、模型信息、父会话链、完整 token 统计：

```sql
CREATE TABLE IF NOT EXISTS sessions (
    id TEXT PRIMARY KEY,
    source TEXT NOT NULL,        -- 'cli', 'telegram', 'discord', 'slack', 'matrix' 等
    user_id TEXT,
    model TEXT,
    model_config TEXT,           -- JSON
    system_prompt TEXT,
    parent_session_id TEXT,      -- 压缩时创建父子链
    started_at REAL NOT NULL,
    ended_at REAL,
    end_reason TEXT,
    message_count INTEGER DEFAULT 0,
    tool_call_count INTEGER DEFAULT 0,
    input_tokens INTEGER DEFAULT 0,
    output_tokens INTEGER DEFAULT 0,
    cache_read_tokens INTEGER DEFAULT 0,
    cache_write_tokens INTEGER DEFAULT 0,
    reasoning_tokens INTEGER DEFAULT 0,
    estimated_cost_usd REAL,     -- 估算成本
    actual_cost_usd REAL,        -- 实际成本
    cost_status TEXT,            -- 'estimated', 'exact', 'both'
    title TEXT,                  -- 唯一标题（非NULL值唯一索引）
    FOREIGN KEY (parent_session_id) REFERENCES sessions(id)
);
```

**messages 表**：完整消息历史，含 reasoning 字段（跨轮次推理链延续）：

```sql
CREATE TABLE IF NOT EXISTS messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT NOT NULL REFERENCES sessions(id),
    role TEXT NOT NULL,
    content TEXT,
    tool_call_id TEXT,
    tool_calls TEXT,              -- JSON
    tool_name TEXT,
    timestamp REAL NOT NULL,
    token_count INTEGER,
    finish_reason TEXT,
    reasoning TEXT,               -- v6: 推理链文本
    reasoning_details TEXT,       -- v6: 结构化推理详情
    codex_reasoning_items TEXT    -- v6: Codex 推理项
);
```

### 9.6.4 FTS5 全文搜索

使用 `messages_fts` 虚拟表实现跨所有会话的全文搜索：

- **自动索引**：INSERT/UPDATE/DELETE 触发器自动维护 FTS 索引
- **查询语法**：支持 FTS5 短语（`"exact phrase"`）、布尔（`OR`/`NOT`）、前缀（`term*`）
- **查询安全**：`_sanitize_fts5_query()` 保护用户输入，防止 FTS5 语法错误
- **上下文片段**：返回匹配消息的前后各 1 条消息作为上下文
- **来源过滤**：可按 `source`（平台）、`exclude_sources`、`role` 过滤

```python
def search_messages(self, query: str, source_filter=None, role_filter=None, limit=20):
    query = self._sanitize_fts5_query(query)
    # 使用 snippet() 函数高亮匹配结果
    sql = """SELECT m.id, m.session_id, m.role,
               snippet(messages_fts, 0, '>>>', '<<<', '...', 40) AS snippet,
               s.source, s.model
            FROM messages_fts
            JOIN messages m ON m.id = messages_fts.rowid
            JOIN sessions s ON s.id = m.session_id
            WHERE messages_fts MATCH ? ..."""
```

### 9.6.5 Schema 版本迁移

`SCHEMA_VERSION = 6`，支持增量迁移：

| 版本 | 变更内容 |
|------|---------|
| v1 | 初始 schema |
| v2 | messages 表添加 `finish_reason` 列 |
| v3 | sessions 表添加 `title` 列 |
| v4 | title 添加唯一索引（仅非NULL） |
| v5 | 添加 11 个计费/token 列（`cache_read_tokens`, `estimated_cost_usd` 等） |
| v6 | messages 表添加 3 个 reasoning 列 |

- **向前兼容**：新版本自动 ALTER TABLE 添加缺失列
- **安全迁移**：每步迁移单独 try/except，已存在的列跳过不报错

### 9.6.6 关键 API

```python
# 会话生命周期
db.create_session(session_id, source="cli", model="claude-sonnet-4-20250514")
db.end_session(session_id, end_reason="user_quit")
db.reopen_session(session_id)  # 清除 ended_at/end_reason

# Token 统计（增量或绝对）
db.update_token_counts(session_id, input_tokens=1500, output_tokens=500, absolute=False)

# 消息存储
db.append_message(session_id, role="user", content="hello")
db.append_message(session_id, role="assistant", tool_calls=[...], reasoning="Let me think...")

# 消息检索
messages = db.get_messages(session_id)                    # 列表，含 JSON 反序列化
conversation = db.get_messages_as_conversation(session_id) # OpenAI 对话格式

# 搜索
results = db.search_messages("docker deployment", source_filter=["cli"])

# 导出与清理
db.export_session(session_id)                           # 单会话导出
db.export_all(source="telegram")                        # 全量导出
db.prune_sessions(older_than_days=90)                   # 清理旧会话（仅已结束的）
```

---

## 九点七、测试基础设施

### 9.7.1 30 秒全局超时

`tests/conftest.py` 使用 `SIGALRM` 实现测试超时保护：

```python
@pytest.fixture(autouse=True)
def _enforce_test_timeout():
    """Kill any individual test that takes longer than 30 seconds.
    SIGALRM is Unix-only; skip on Windows."""
    if sys.platform == "win32":
        yield; return
    old = signal.signal(signal.SIGALRM, _timeout_handler)
    signal.alarm(30)
    yield
    signal.alarm(0)
    signal.signal(signal.SIGALRM, old)
```

### 9.7.2 自动使用 Fixtures

```python
# 1. _isolate_hermes_home — 每个测试重定向到临时目录，永不写入 ~/.hermes/
@pytest.fixture(autouse=True)
def _isolate_hermes_home(tmp_path, monkeypatch):
    fake_home = tmp_path / "hermes_test"
    fake_home.mkdir()
    (fake_home / "sessions").mkdir()
    monkeypatch.setenv("HERMES_HOME", str(fake_home))
    # Reset plugin singleton, del session platform env vars, del OPENROUTER_API_KEY

# 2. _ensure_current_event_loop — 确保同步测试有可用的事件循环
@pytest.fixture(autouse=True)
def _ensure_current_event_loop(request):
    # 为 asyncio.get_event_loop().run_until_complete(...) 调用提供 loop

# 3. _enforce_test_timeout — 30s SIGALRM 保护

# 4. tmp_dir — 提供临时目录
@pytest.fixture()
def tmp_dir(tmp_path):
    return tmp_path

# 5. mock_config — 最小化配置字典
@pytest.fixture()
def mock_config():
    return {
        "model": "test/mock-model",
        "toolsets": ["terminal", "file"],
        "max_turns": 10,
        "terminal": {"backend": "local", "cwd": "/tmp", "timeout": 30},
        "compression": {"enabled": False},
        "memory": {"memory_enabled": False, "user_profile_enabled": False},
        "command_allowlist": [],
    }
```

### 9.7.3 pytest 配置

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: marks tests requiring external services (API keys, Modal, etc.)"]
addopts = "-m 'not integration' -n auto"
```

- **`-n auto`**：使用 `pytest-xdist` 并行执行测试
- **默认跳过集成测试**：`-m 'not integration'` 确保 CI 不依赖外部服务
- **project root 自动加入 path**：`conftest.py` 确保项目根目录可导入

---

## 九点八、CI/CD 流水线

项目有 8 个 GitHub Actions workflow 文件，位于 `.github/workflows/`：

### 9.8.1 tests.yml — 测试流水线

- **触发**：push/PR 到 main 分支
- **并行 Job**：`test`（单元测试）+ `e2e`（端到端测试）
- **环境**：ubuntu-latest, Python 3.11, uv 安装依赖
- **超时**：10 分钟
- **并发控制**：同 PR/分支的旧运行自动取消
- **命令**：`pytest tests/ -q --ignore=tests/integration --ignore=tests/e2e --tb=short -n auto`
- **安全**：`OPENROUTER_API_KEY=""`, `OPENAI_API_KEY=""`, `NOUS_API_KEY=""` 确保测试不调用真实 API

### 9.8.2 docker-publish.yml — Docker 多架构构建发布

- **触发**：push/PR/release 到 main
- **限定仓库**：仅 `NousResearch/hermes-agent`（fork 不运行）
- **流程**：amd64 构建 → smoke test（`docker run --help`）→ 多架构推送（linux/amd64 + linux/arm64）
- **Tag**：main 分支为 `latest`，release 为版本 tag
- **缓存**：GitHub Actions cache（`type=gha,mode=max`）

### 9.8.3 supply-chain-audit.yml — 供应链安全审计

PR 级别的自动安全扫描，检测可疑模式：
- .pth 文件添加（litellm 供应链攻击的精确机制）
- base64 decode + exec/eval 组合
- subprocess 调用编码/混淆命令
- CI/CD workflow 文件修改
- Docker 构建文件修改
- 依赖清单文件修改
- GitHub Actions 版本 tag 未 SHA 锁定
- 发现关键模式时自动失败 PR

### 9.8.4 其他 Workflow

| Workflow | 用途 |
|----------|------|
| `contributor-check.yml` | 验证 PR 贡献者 email 在 AUTHOR_MAP 中 |
| `deploy-site.yml` | 构建并部署 Docusaurus 文档站到 GitHub Pages |
| `docs-site-checks.yml` | PR 级别文档站构建检查 + ASCII 图 lint |
| `nix.yml` | Nix flake 构建测试（ubuntu + macOS） |
| `skills-index.yml` | 构建 skills 索引 |

---

## 十、面试八股文

### 10.1 pyproject.toml vs setup.py
| 维度 | pyproject.toml | setup.py |
|------|---------------|----------|
| 格式 | TOML（标准，人类可读） | Python可执行脚本 |
| 声明式 | 是，所有配置静态声明 | 否，可动态生成依赖 |
| 标准化 | PEP 621官方标准 | 事实上的标准，无正式规范 |
| 构建后端 | 可配置支持setuptools/poetry/hatch等 | 固定使用setuptools |
| 安全 | 无执行代码风险 | 安装时执行任意代码，存在安全隐患 |
| 工具支持 | 所有现代Python工具支持（pip, poetry, pdm等） | 遗留工具支持 |
| 扩展能力 | 支持自定义工具配置（如pytest, black等） | 需要额外setup.cfg配合 |

### 10.2 Docker 最佳实践
1. **多阶段构建**：构建阶段和运行阶段分离，减小镜像体积
2. **非root用户运行**：最小权限原则，避免容器逃逸风险
3. **最小基础镜像**：使用slim/alpine镜像，减少攻击面
4. **层缓存优化**：COPY requirements在前，COPY代码在后，利用Docker层缓存
5. **健康检查**：配置HEALTHCHECK指令，支持容器健康监测
6. **持久化数据卷**：数据存储在volume中，不存储在容器可写层
7. **.dockerignore**：忽略不必要的文件，减少构建上下文大小
8. **单进程原则**：一个容器只运行一个主进程，便于日志收集和故障排查
9. **环境变量配置**：配置通过环境变量注入，不硬编码到镜像
10. **镜像签名**：启用镜像签名，防止篡改和供应链攻击

### 10.3 定时任务方案对比
| 方案 | 优点 | 缺点 |
|------|------|------|
| APScheduler | 轻量级，进程内集成，灵活的调度规则 | 不支持分布式，进程崩溃任务丢失 |
| Celery Beat | 分布式，高可用，支持任务结果存储 | 重度依赖RabbitMQ/Redis，运维复杂 |
| 系统cron | 系统级可靠，无需额外进程 | 功能有限，不支持动态任务，跨平台适配差 |
| Kubernetes CronJob | 分布式，高可用，自动重试 | 依赖K8s集群，重运维，调度粒度最小1分钟 |
| Hermes 自建 | 轻量级，跨平台，动态任务，多平台投递 | 功能有限，适合中小型场景 |

### 10.4 并发编程选型
| 方式 | 适用场景 | 优势 | 劣势 |
|------|---------|------|------|
| multiprocessing | CPU密集型任务 | 真正并行，不受GIL限制 | 进程间通信开销大，内存占用高 |
| threading | I/O密集型任务，低延迟要求 | 轻量，共享内存，上下文切换快 | 受GIL限制，CPU密集型无法并行 |
| asyncio | 高并发I/O场景（网络请求、API调用） | 极高并发，低内存占用，单线程无锁 | 所有IO必须异步，生态兼容性差，调试困难 |
| concurrent.futures | 混合场景，任务抽象 | 统一API，便于切换线程/进程实现 | 灵活性较低，复杂场景不够用 |

### 10.5 12-factor App 配置管理
1. **配置存储在环境变量中**：与代码完全分离，不同环境配置不同
2. **严格区分配置与代码**：配置不随代码版本变化，代码相同部署到不同环境无需修改
3. **环境间差异最小化**：各环境配置结构一致，仅值不同
4. **无敏感信息硬编码**：密钥、密码等敏感信息永远不进入代码库
5. **配置统一管理**：使用配置中心统一管理配置，避免分散在各处
6. **运行时注入**：配置在进程启动时注入，运行期间不修改

### 10.6 原子写入的三步保障

实际的原子写入实现在 `utils.py` 中，提供 `atomic_json_write()` 和 `atomic_yaml_write()` 两个函数：

```python
def atomic_json_write(path: Union[str, Path], data: Any, *, indent: int = 2, **dump_kwargs) -> None:
    path = Path(path)
    path.parent.mkdir(parents=True, exist_ok=True)

    # 1. 在目标文件同一目录下创建临时文件（使用 tempfile.mkstemp 避免命名冲突）
    fd, tmp_path = tempfile.mkstemp(
        dir=str(path.parent),
        prefix=f".{path.stem}_",
        suffix=".tmp",
    )
    try:
        with os.fdopen(fd, "w", encoding="utf-8") as f:
            json.dump(data, f, indent=indent, ensure_ascii=False, **dump_kwargs)
            f.flush()
            os.fsync(f.fileno())       # 确保数据落盘

        # 2. POSIX 原子替换，要么成功要么失败，无中间状态
        os.replace(tmp_path, path)

    except BaseException:
        # 3. BaseException 捕获（含 KeyboardInterrupt/SystemExit），确保清理后再抛出
        try:
            os.unlink(tmp_path)
        except OSError:
            pass
        raise
```

**与文档中简化版本的关键差异：**

| 维度 | 文档简化版 | 实际代码 |
|------|----------|---------|
| 临时文件创建 | `path.with_suffix(".tmp")` | `tempfile.mkstemp(dir=parent, prefix=".{stem}_", suffix=".tmp")` |
| 所在目录 | 同目录（可能跨文件系统） | **强制在目标文件父目录**（确保 `os.replace` 是原子操作——同一文件系统内） |
| 命名冲突 | 无保护 | `mkstemp` 保证唯一文件名 |
| 内容格式 | 原始字符串 `f.write(content)` | JSON/YAML 序列化（`json.dump` / `yaml.dump`） |
| 异常处理 | 异常处理在 try/except 内部，`os.unlink` 可能被跳过 | `BaseException` 捕获 + finally 风格清理 |

- **第一步**：写入临时文件并fsync，确保数据真正写入磁盘，不是在页缓存
- **第二步**：`os.replace` 是 POSIX 标准原子操作，同一文件系统内保证原子性
- **第三步**：捕获所有异常（包括 KeyboardInterrupt），确保临时文件被清理，避免垃圾文件堆积

---

## 十一、面试官可能问的问题

### Q1: 为什么使用原子写入？
`open("w")` 模式会立即截断文件，如果进程在写入过程中崩溃，文件会变成空或内容不完整。temp文件+os.replace的原子写入模式保证读取者要么看到完整的旧文件，要么看到完整的新文件，没有中间状态，确保数据一致性。

### Q2: Profile 系统如何实现隔离？
每个Profile有独立的 `HERMES_HOME` 目录，所有配置、技能、记忆、会话数据都存储在该目录下完全隔离。`get_subprocess_home()` 为子进程提供独立的HOME目录，确保git/ssh/gh等工具的配置、密钥完全隔离，不污染系统用户环境。Python进程自身环境不修改，仅在子进程启动时注入环境变量，安全无副作用。

### Q3: 日志系统如何脱敏？
`RedactingFormatter` 在日志格式化后、写入磁盘前，使用多层正则匹配替换敏感信息：
1. 已知API密钥前缀匹配（sk-*, ghp_*, xoxb-*等30+种）
2. 环境变量赋值匹配（API_KEY=*, TOKEN=*等）
3. JSON敏感字段匹配（"apiKey": ".*", "token": ".*"等）
4. Authorization头匹配
5. Telegram bot token、私钥块、数据库连接字符串专项匹配
6. JWT令牌、电话号码、Discord用户ID等隐私信息匹配

短令牌完全替换，长令牌保留前6后4位便于调试，确保敏感信息永远不落盘。

### Q4: 定时任务如何防重复执行？
使用跨平台文件锁（Unix下fcntl，Windows下msvcrt），`LOCK_EX | LOCK_NB` 非阻塞排他锁。如果获取锁失败，说明已有其他进程在执行tick，直接跳过。确保同一时间只有一个进程执行定时任务，避免重复运行。

### Q5: 定时任务如何防崩溃重放？
周期性任务在执行前先将`next_run_at`推进到下一次调度时间，然后再执行任务。即使进程在执行过程中崩溃，下次启动时任务的`next_run_at`已经是未来时间，不会重复执行已经处理过的任务。将调度语义从"至少一次"转变为"最多一次"，避免重复执行带来的副作用。一次性任务不提前推进，允许重启后重试。

### Q6: 批处理的并行粒度为什么是批不是 prompt？
每个Prompt需要独立的AIAgent实例和完整的agent loop，包含多次工具调用和LLM请求。如果并行粒度是单个Prompt，会导致大量并发的LLM API请求和工具进程，容易触发API限流和系统资源竞争。Batch级并行可以更好地控制并发度，每个Batch顺序执行内部Prompt，在吞吐量和资源使用之间取得平衡。

### Q7: Checkpoint 的基于内容 resume？
传统基于索引的resume在数据集修改或排序变化时会错误跳过任务。基于内容的resume通过计算Prompt内容哈希，匹配已完成的任务，即使索引变化或数据集修改也能正确识别已完成的任务。同时支持两种模式，确保在各种场景下都能正确恢复进度。

### Q8: 轨迹压缩的保护策略？
采用首尾保护策略：
1. **头部保护**：系统提示、用户初始请求、首条Assistant回复、首次工具调用，这些是对话的基础上下文，必须完整保留
2. **尾部保护**：最后N轮对话（默认4轮），保留最近的上下文，确保后续回复一致性
3. **中间压缩**：仅压缩中间的对话轮次，从头开始累积直到达到需要节省的Token量，最大化保留最近信息
4. **工具完整性**：所有工具调用的元数据和结果都保留，确保工具状态一致性，不影响后续工具使用

### Q9: WSL 检测为什么读 /proc/version？
WSL1的内核版本字符串包含"Microsoft"标识，WSL2包含"WSL"标识，这是最可靠的跨版本检测方式。其他检测方式如检查`/proc/sys/fs/binfmt_misc/WSLInterop`存在性在某些受限环境中不可用，而读取`/proc/version`几乎在所有环境中都有权限。检测结果进程级缓存，避免重复IO。

### Q10: IPv4 偏好为什么 monkey-patch getaddrinfo？
某些网络环境中IPv6配置错误或不可用，但系统默认优先尝试IPv6，导致TCP连接超时（通常30秒以上）。通过monkey-patch系统`socket.getaddrinfo`函数，在请求未指定协议族时优先返回IPv4地址，可以大幅提升网络请求速度，同时保持对纯IPv6环境的兼容（IPv4解析失败时自动回退）。这种方式对上层应用完全透明，无需修改业务代码。

### Q11: 日志的会话上下文如何注入？
使用`logging.setLogRecordFactory()`替换全局日志记录工厂，在每个LogRecord创建时自动注入`session_tag`字段，值为当前线程的`session_id`（如果存在）。这种方式比Filter更可靠，因为Filter是Handler级别的，而记录工厂是全局的，所有日志（包括第三方库日志）都会包含session_tag，实现全链路会话跟踪。

### Q12: 噪音抑制如何实现？
在`setup_logging()`中将15+个常用第三方库的日志级别统一设为WARNING，包括openai、httpx、httpcore、asyncio、grpc等。这些库在INFO/DEBUG级别会产生大量无用日志，抑制后可以保持日志简洁，突出业务相关信息。verbose模式下仍然可以临时启用DEBUG级日志。

### Q13: 托管模式如何影响权限？
托管模式（如NixOS部署）中，状态目录使用setgid权限（2770），确保新创建的文件继承目录的组所有权。自定义`_ManagedRotatingFileHandler`在文件创建和日志轮转时自动将权限设为0o660，确保网关进程和交互式CLI用户（同组）都能读写日志文件，避免权限错误。

### Q14: SWE 基准测试的意义？
SWE-bench是行业标准的软件工程Agent基准测试，包含真实的GitHub issues和代码库，评估AI Agent解决实际软件工程问题的能力。Hermes支持三种后端运行测试：
1. **本地后端**：直接在本地机器运行，速度快，适合开发调试
2. **Docker后端**：每个任务在独立Docker容器中运行，环境隔离，安全
3. **Modal后端**：云端分布式运行，支持大规模并行测试，速度快

测试结果自动生成分数和详细报告，便于迭代改进Agent能力。

### Q15: 如何设计一个可靠的定时任务系统？
1. **文件锁防重复**：跨平台文件锁确保同一时间只有一个调度实例运行
2. **执行前推进调度时间**：防崩溃重放，避免重复执行
3. **优雅窗口处理错过的任务**：避免重启后的任务风暴
4. **内置脚本注入支持**：任务运行前执行自定义脚本，动态注入上下文
5. **多平台结果投递**：支持18+个消息平台，任务结果自动投递
6. **不活动超时机制**：检测无活动的挂起任务，自动终止，避免资源泄漏
7. **原子状态更新**：任务状态持久化使用原子写入，避免数据损坏
8. **幂等设计**：任务执行应该是幂等的，即使重复执行也不会产生副作用

---

## 十二、学习路径建议

1. **基础层**：`hermes_constants.py` -- 路径解析和环境检测，了解底层基础抽象
2. **可观测性**：`hermes_logging.py` -- 日志系统，学习可观测性设计
3. **工具函数**：`utils.py` -- 原子写入和通用工具函数，了解工程实践
4. **时间处理**：`hermes_time.py` -- 时区处理和时间工具，了解跨时区设计
5. **定时任务**：`cron/jobs.py` -- Job CRUD和持久化，了解数据建模
6. **调度引擎**：`cron/scheduler.py` -- 调度执行引擎，了解容错和分布式设计
7. **批处理**：`batch_runner.py` -- 批处理系统，了解并行设计和大规模任务处理
8. **轨迹压缩**：`trajectory_compressor.py` -- 轨迹压缩，了解LLM应用和Token优化